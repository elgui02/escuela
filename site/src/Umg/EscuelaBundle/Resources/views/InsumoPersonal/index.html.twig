{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InsumoPersonal list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidad</th>
                <th>Retornado</th>
                <th>Insumo_id</th>
                <th>Personal_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('insumopersonal_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Cantidad }}</td>
                <td>{{ entity.Retornado }}</td>
                <td>{{ entity.Insumoid }}</td>
                <td>{{ entity.Personalid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('insumopersonal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('insumopersonal_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('insumopersonal_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
