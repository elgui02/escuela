{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AlumnoResponsable list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Alumno_id</th>
                <th>Responsable_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('alumnoresponsable_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Alumnoid }}</td>
                <td>{{ entity.Responsableid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('alumnoresponsable_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('alumnoresponsable_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('alumnoresponsable_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
