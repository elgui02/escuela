{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AlumnoResponsable</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Alumno_id</th>
                <td>{{ entity.Alumnoid }}</td>
            </tr>
            <tr>
                <th>Responsable_id</th>
                <td>{{ entity.Responsableid }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('alumnoresponsable') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('alumnoresponsable_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
